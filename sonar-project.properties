sonar.projectKey=emadataei_codeEvalTooling
sonar.organization=emadataei

# Disable test coverage requirements
sonar.coverage.exclusions=**/*
sonar.exclusions=**/test_*,**/tests/**,**/*_test.*,**/*Test.*,**/*.test.*

# Disable coverage gates
sonar.qualitygate.wait=false

# Disable SonarCloud PR decoration to avoid conflicts with our quality gate
sonar.pullrequest.github.summary_comment=false
sonar.pullrequest.github.endpoint=
sonar.pullrequest.provider=

# Disable test coverage requirements
sonar.coverage.exclusions=**/*
sonar.exclusions=**/test_*,**/tests/**,**/*_test.*,**/*Test.*,**/*.test.*

# Disable coverage gates
sonar.qualitygate.wait=false

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=codeEvalTooling
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
